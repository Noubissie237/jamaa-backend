type Account {
  id: ID!
  userId: ID!
  accountNumber: String!
  balance: Float!
  createdAt: String!
}


type Mutation {
    createAccount(userId: ID!): Account
    i
}

type Query {
    getAllAccounts: [Account!]
}